'use client';import {useEffect,useState} from 'react';export default function InspectionCountdown({deadline}:{deadline:string}){const [left,setLeft]=useState(Math.max(0,new Date(deadline).getTime()-Date.now()));useEffect(()=>{const id=setInterval(()=>setLeft(Math.max(0,new Date(deadline).getTime()-Date.now())),1000);return()=>clearInterval(id)},[deadline]);const s=Math.floor(left/1000),h=Math.floor(s/3600),m=Math.floor((s%3600)/60),sec=s%60;return <div className="countdown">{h}h {String(m).padStart(2,'0')}m {String(sec).padStart(2,'0')}s <span>remaining</span></div>}