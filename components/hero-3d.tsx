'use client';
import { useEffect, useRef } from 'react';
export default function Hero3D(){const ref=useRef<HTMLDivElement>(null);useEffect(()=>{const el=ref.current;if(!el)return;const move=(e:MouseEvent)=>{const x=(e.clientX/innerWidth-.5)*2,y=(e.clientY/innerHeight-.5)*2;el.style.setProperty('--mx',`${x*8}deg`);el.style.setProperty('--my',`${y*-8}deg`)};addEventListener('mousemove',move);return()=>removeEventListener('mousemove',move)},[]);return <div ref={ref} className="hero-3d" aria-hidden="true"><div className="orbital o1"/><div className="orbital o2"/><div className="orbital o3"/><div className="core"><div className="core-grid"/><strong>IC</strong><span>PROCUREMENT<br/>NETWORK</span></div>{['RFQ','₹50L+','48H','QC'].map((x,i)=><div className={`node n${i+1}`} key={x}>{x}</div>)}<div className="scan"/></div>}