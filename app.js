const { createClient } = window.supabase || {};
const cfg = window.INDUSTRIALCONNECT_SUPABASE || {};
const live = !!(createClient && cfg.url && cfg.anonKey);
const db = live ? createClient(cfg.url, cfg.anonKey) : null;
const app = document.getElementById('app');
const categories = ['CNC Turning','CNC Milling / VMC','CNC Job Work','Investment Casting','Fabrication','Laser Cutting','Sheet Metal','Grinding','Forging','Die Casting','Injection Moulding','Heat Treatment','Surface Treatment','Assembly'];
const slugs = {'CNC Turning':'cnc-turning','CNC Milling / VMC':'cnc-milling-vmc','CNC Job Work':'cnc-job-work','Investment Casting':'investment-casting','Fabrication':'fabrication','Laser Cutting':'laser-cutting','Sheet Metal':'sheet-metal','Grinding':'grinding','Forging':'forging','Die Casting':'die-casting','Injection Moulding':'injection-moulding','Heat Treatment':'heat-treatment','Surface Treatment':'surface-treatment','Assembly':'assembly'};
const demo = { user:null, role:'buyer', rfqs:JSON.parse(localStorage.getItem('ic_v2_rfqs')||'[]'), quotes:JSON.parse(localStorage.getItem('ic_v2_quotes')||'[]'), orders:JSON.parse(localStorage.getItem('ic_v2_orders')||'[]') };
let sessionUser = null;

function esc(v=''){return String(v).replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[c]));}
function money(v){return '₹'+Number(v||0).toLocaleString('en-IN',{maximumFractionDigits:2});}
function slugify(v){return String(v).toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'');}
function saveDemo(){localStorage.setItem('ic_v2_rfqs',JSON.stringify(demo.rfqs));localStorage.setItem('ic_v2_quotes',JSON.stringify(demo.quotes));localStorage.setItem('ic_v2_orders',JSON.stringify(demo.orders));}
function toast(msg){const t=document.createElement('div');t.className='toast';t.textContent=msg;document.body.appendChild(t);setTimeout(()=>t.remove(),2800);}

function nav(){return `<nav class="nav"><div class="container navin"><button class="brand" onclick="go('home')"><span class="mark">IC</span><span>IndustrialConnect</span></button><div class="navlinks"><a href="#how">How it works</a><a href="#capabilities">Capabilities</a><a href="#suppliers">Manufacturers</a></div><div class="actions">${sessionUser?`<button class="btn ghost" onclick="go('dashboard')">Workspace</button><button class="btn" onclick="logout()">Sign out</button>`:`<button class="btn ghost" onclick="openAuth('signin')">Sign in</button><button class="btn primary" onclick="openAuth('signup')">Get started</button>`}</div></div></nav>`}
function home(){return `${nav()}<main><section class="hero"><div class="container heroGrid"><div><span class="eyebrow"><i class="dot"></i> India-first industrial procurement</span><h1>Manufacturing, without the procurement headache.</h1><p>Your requirement. The right manufacturer. One workflow — from RFQ and competitive quotes to production, quality and delivery.</p><div class="heroBtns"><button class="btn primary" onclick="openRFQ()">Post a requirement →</button><button class="btn" onclick="openAuth('signup','supplier')">I'm a manufacturer</button></div><div class="trust"><span>✓ Verified manufacturers</span><span>✓ Structured quotations</span><span>✓ Order visibility</span></div></div><div class="preview"><div class="window"><div class="windowbar"><i></i><i></i><i></i></div><div class="mock"><div class="miniTitle">Order Room · IC-2026-000128</div><div class="miniSub">CNC turned shaft · 2,000 pcs</div><div class="rfqcard"><span class="badge">IN PRODUCTION</span><div class="progress"><span style="width:62%"></span></div><div class="row"><b>Production</b><strong>62%</strong></div><div class="miniTimeline"><span>✓ Material received</span><span>● Production in progress</span><span>○ Quality inspection</span></div></div><div class="rfqcard"><div class="row"><b>3 manufacturers matched</b><span>Live RFQ</span></div><div class="quoteLine"><span>Rajkot Precision Works</span><b>₹148 / pc</b></div><div class="quoteLine"><span>Apex VMC Manufacturing</span><b>₹152 / pc</b></div></div></div></div></div></div></section><section id="how"><div class="container"><div class="sectionHead"><span class="eyebrow">One workflow</span><h2>Requirement to delivered parts.</h2><p>Buyers get visibility. Manufacturers get relevant jobs.</p></div><div class="flow">${[['01','Requirement','Upload drawing, quantity, material and delivery.'],['02','Matching','Match process, capability and verified suppliers.'],['03','Quotes','Compare structured manufacturer quotations.'],['04','Production','Track milestones, QC documents and shipment.'],['05','Delivery','Accept, dispute when needed and complete.']].map(x=>`<div class="step"><span class="num">${x[0]}</span><h3>${x[1]}</h3><p>${x[2]}</p></div>`).join('')}</div></div></section><section id="capabilities" class="features"><div class="container"><div class="sectionHead"><span class="eyebrow">Built for industry</span><h2>Everything important, in one place.</h2></div><div class="featureGrid">${[['RFQ-first procurement','Describe the job instead of searching through generic supplier listings.'],['Verified supplier profiles','Capabilities, machines, certifications and delivery signals.'],['Structured quotations','Price, tooling, material, setup, packaging, inspection, shipping and GST.'],['Order Room','Production updates, quality documents, dispatch and messages stay attached.'],['Exception control','Missing QC, late production and delivery issues surface early.'],['Commission tracking','Orders calculate platform fee and supplier payout server-side.']].map((x,i)=>`<div class="feature"><div class="icon">${String(i+1).padStart(2,'0')}</div><h3>${x[0]}</h3><p>${x[1]}</p></div>`).join('')}</div></div></section><section id="suppliers"><div class="container"><div class="sectionHead"><span class="eyebrow">Manufacturer network</span><h2>Relevant jobs, not random leads.</h2><p>Suppliers receive matched requirements instead of a generic lead feed.</p></div><div class="featureGrid"><div class="feature"><span class="badge">CNC</span><h3>Turning & VMC</h3><p>Precision machining, repeat production and job work.</p></div><div class="feature"><span class="badge">Casting</span><h3>Investment Casting</h3><p>Near-net-shape components with controlled quality workflow.</p></div><div class="feature"><span class="badge">Fabrication</span><h3>Fabrication & Sheet Metal</h3><p>Laser cutting, bending, welding and assemblies.</p></div></div></div></section><section><div class="container"><div class="cta"><h2>Ready to run your first RFQ?</h2><p>Start with one requirement. IndustrialConnect handles the workflow.</p><button class="btn teal" onclick="openRFQ()">Get manufacturer quotes →</button></div></div></section></main><footer class="footer"><div class="container">© 2026 IndustrialConnect India · B2B manufacturing procurement</div></footer>`}

function authModal(mode='signin',prefRole='buyer'){const signup=mode==='signup';const m=document.createElement('div');m.className='modalWrap';m.innerHTML=`<div class="modal auth"><div class="modalHead"><div><span class="eyebrow">IndustrialConnect</span><b>${signup?'Create your workspace':'Welcome back'}</b><small>${live?'Live Supabase authentication':'Demo mode — add Supabase config for live auth'}</small></div><button class="close" onclick="this.closest('.modalWrap').remove()">×</button></div><div class="modalBody"><form id="authForm" class="formGrid">${signup?`<div class="field full"><label>Full name *</label><input name="name" required placeholder="Your name"></div><div class="field full"><label>Company name *</label><input name="company" required placeholder="ABC Engineering Pvt Ltd"></div>`:''}<div class="field full"><label>Email *</label><input name="email" type="email" required placeholder="you@company.com"></div><div class="field full"><label>Password *</label><input name="password" type="password" minlength="6" required placeholder="Minimum 6 characters"></div>${signup?`<div class="field full"><label>I am a</label><div class="rolePick"><label><input type="radio" name="role" value="buyer" ${prefRole!=='supplier'?'checked':''}> Buyer</label><label><input type="radio" name="role" value="supplier" ${prefRole==='supplier'?'checked':''}> Manufacturer</label></div></div>`:''}<div class="field full"><button class="btn teal" type="submit">${signup?'Create account':'Sign in'} →</button></div><div class="field full center"><button type="button" class="textBtn" id="switchAuth">${signup?'Already have an account? Sign in':'New to IndustrialConnect? Create account'}</button></div></form></div></div>`;document.body.appendChild(m);m.querySelector('#switchAuth').onclick=()=>{m.remove();openAuth(signup?'signin':'signup',prefRole)};m.querySelector('#authForm').onsubmit=e=>handleAuth(e,m,signup);}
async function handleAuth(e,m,signup){e.preventDefault();const f=new FormData(e.target);const email=f.get('email').trim();const password=f.get('password');if(!live){demo.user={id:'demo-user',email,name:f.get('name')||'Demo User',company:f.get('company')||'Demo Company'};demo.role=signup?(f.get('role')||'buyer'):'buyer';sessionUser=demo.user;m.remove();go('dashboard');toast('Demo account ready. Connect Supabase for live accounts.');return;}try{if(signup){const {data,error}=await db.auth.signUp({email,password,options:{data:{full_name:f.get('name')}}});if(error)throw error;if(data.user){await db.from('companies').insert({name:f.get('company')});toast('Account created. Check email if confirmation is enabled.');}}else{const {error}=await db.auth.signInWithPassword({email,password});if(error)throw error;}m.remove();await loadSession();}catch(err){toast(err.message||'Authentication failed');}}
function openAuth(mode='signin',role='buyer'){authModal(mode,role)}
async function loadSession(){if(!live){sessionUser=demo.user;render();return;}const {data}=await db.auth.getSession();sessionUser=data.session?.user||null;if(sessionUser){const {data:p}=await db.from('profiles').select('role,company_id,full_name').eq('id',sessionUser.id).single();demo.role=p?.role||'buyer';}render();}
async function logout(){if(live)await db.auth.signOut();demo.user=null;sessionUser=null;go('home');toast('Signed out');}

function dashboard(){if(!sessionUser){openAuth('signin');return '';}const role=demo.role||'buyer';if(role==='admin')return adminDash();if(role==='supplier')return supplierDash();return buyerDash();}
function dashShell(title,sub,body){return `${nav()}<main class="dash"><div class="container"><div class="dashTop"><div><span class="eyebrow">${esc(demo.role)} workspace</span><h1>${title}</h1><p>${sub}</p></div><div class="dashActions"><button class="btn" onclick="openAuth('signup','supplier')">Invite team</button>${demo.role==='buyer'?`<button class="btn primary" onclick="openRFQ()">Post requirement</button>`:''}</div></div>${body}</div></main>`}
function buyerDash(){const rs=demo.rfqs;return dashShell('Source smarter. Track every order.','Your requirements, quotes and production status in one workspace.',`<div class="tabs"><button class="tab active">Overview</button><button class="tab" onclick="openOrders()">Orders</button><button class="tab" onclick="openMessages()">Messages</button><button class="tab">Account</button></div><div class="stats"><div class="stat"><small>Active RFQs</small><strong>${rs.length}</strong></div><div class="stat"><small>Quotes received</small><strong>${demo.quotes.length}</strong></div><div class="stat"><small>Active orders</small><strong>${demo.orders.filter(o=>!['completed','cancelled'].includes(o.status)).length}</strong></div><div class="stat"><small>Completed</small><strong>${demo.orders.filter(o=>o.status==='completed').length}</strong></div></div><div class="panelGrid"><div class="panel"><div class="panelTitle"><h3>Recent requirements</h3><span class="muted">${rs.length?'Latest first':'Start here'}</span></div>${rs.length?rs.slice().reverse().map(r=>rfqRow(r)).join(''):`<div class="empty"><div class="emptyIcon">+</div><h3>No requirements yet</h3><p>Post a drawing or manufacturing specification and start matching.</p><button class="btn primary" onclick="openRFQ()">Post first requirement</button></div>`}</div><div class="panel"><div class="panelTitle"><h3>Order Room</h3><button class="textBtn" onclick="openOrders()">View all →</button></div>${demo.orders.length?demo.orders.slice(-2).reverse().map(orderCard).join(''):`<div class="empty compact"><p>Your accepted quotes become tracked orders here.</p></div>`}</div></div>`)}
function supplierDash(){return dashShell('Matched manufacturing jobs.','Only requirements matched to your process and capability should appear here.',`<div class="tabs"><button class="tab active">Matched jobs</button><button class="tab" onclick="openSupplierOrders()">Orders</button><button class="tab">Messages</button><button class="tab">Company profile</button></div><div class="stats"><div class="stat"><small>Matched RFQs</small><strong>8</strong></div><div class="stat"><small>Quotes submitted</small><strong>${demo.quotes.length}</strong></div><div class="stat"><small>Active orders</small><strong>3</strong></div><div class="stat"><small>On-time rate</small><strong>97%</strong></div></div><div class="panel"><div class="panelTitle"><h3>Jobs for your capability</h3><span class="badge">Verified matching</span></div>${demo.rfqs.length?demo.rfqs.slice().reverse().map((r,i)=>`<div class="job"><div><b>${esc(r.title)}</b><span>${esc(r.category)} · ${esc(r.quantity)} ${esc(r.unit)} · ${esc(r.material)} · ${esc(r.location||'India')}</span><small>Match score ${92-i*4}% · Drawing/spec available</small></div><button class="btn" onclick="openQuote('${r.id}')">${demo.quotes.find(q=>q.rfqId===r.id)?'Edit quote':'Submit quote'} →</button></div>`).join(''):`<div class="empty"><h3>No matched RFQs</h3><p>Complete your capability and machine profile to improve matching.</p></div>`}</div>`)}
function adminDash(){const revenue=demo.orders.reduce((a,o)=>a+Number(o.platformFee||0),0);return dashShell('Control Tower','Handle exceptions, verification, transactions and commission without touching every normal order.',`<div class="stats"><div class="stat"><small>GMV</small><strong>${money(demo.orders.reduce((a,o)=>a+Number(o.total||0),0))}</strong></div><div class="stat"><small>Platform revenue</small><strong>${money(revenue)}</strong></div><div class="stat"><small>Open RFQs</small><strong>${demo.rfqs.length}</strong></div><div class="stat"><small>Pending payouts</small><strong>${demo.orders.filter(o=>o.payoutStatus!=='paid').length}</strong></div></div><div class="exception"><div><span class="statusDot green"></span><b>Normal</b><p>Core workflow operational</p></div><div><span class="statusDot amber"></span><b>Attention</b><p>Review supplier verification and QC exceptions</p></div><div><span class="statusDot red"></span><b>Critical</b><p>Disputes or failed payouts require action</p></div></div><div class="panelGrid"><div class="panel"><div class="panelTitle"><h3>RFQ operations</h3><button class="textBtn" onclick="adminRFQs()">Open queue →</button></div>${demo.rfqs.slice(-5).reverse().map(r=>rfqRow(r,true)).join('')||'<div class="empty compact">No RFQs yet.</div>'}</div><div class="panel"><div class="panelTitle"><h3>Commission ledger</h3><span class="badge">Default 2%</span></div>${demo.orders.map(o=>`<div class="ledger"><div><b>${esc(o.orderNumber)}</b><span>${esc(o.title)}</span></div><div><strong>${money(o.platformFee)}</strong><small>${o.commission||2}% · ${esc(o.payoutStatus||'pending')}</small></div></div>`).join('')||'<div class="empty compact">Orders will appear here after quote acceptance.</div>'}</div></div>`)}
function rfqRow(r,admin=false){return `<div class="job"><div><b>${esc(r.title)}</b><span>${esc(r.category)} · ${esc(r.quantity)} ${esc(r.unit)} · ${esc(r.material)}</span><small>${esc(r.status||'matching')} · ${esc(r.location||'India')}</small></div><button class="btn" onclick="${admin?`adminRFQ('${r.id}')`:`openRFQDetail('${r.id}')`}">View →</button></div>`}
function orderCard(o){return `<div class="orderMini" onclick="openOrder('${o.id}')"><div class="row"><b>${esc(o.orderNumber)}</b><span class="badge">${esc((o.status||'').replaceAll('_',' '))}</span></div><p>${esc(o.title)} · ${esc(o.quantity)} pcs</p><div class="progress"><span style="width:${Number(o.progress||20)}%"></span></div><div class="row"><small>Production</small><small>${Number(o.progress||20)}%</small></div></div>`}

function openRFQ(){if(!sessionUser){openAuth('signin');return;}const m=document.createElement('div');m.className='modalWrap';m.innerHTML=`<div class="modal"><div class="modalHead"><div><span class="eyebrow">New RFQ</span><b>Post a manufacturing requirement</b><small>Upload the specification once. We structure the requirement for suppliers.</small></div><button class="close" onclick="this.closest('.modalWrap').remove()">×</button></div><div class="modalBody"><form id="rfqForm" class="formGrid"><div class="field full"><label>Requirement title *</label><input name="title" required maxlength="180" placeholder="e.g. CNC turned shaft — EN8"></div><div class="field"><label>Process *</label><select name="category">${categories.map(c=>`<option value="${slugs[c]}">${c}</option>`).join('')}</select></div><div class="field"><label>Quantity *</label><input name="quantity" type="number" min="1" step="0.01" required placeholder="2000"></div><div class="field"><label>Unit</label><select name="unit"><option>pcs</option><option>kg</option><option>sets</option><option>units</option></select></div><div class="field"><label>Material *</label><input name="material" required placeholder="EN8 / SS304 / Aluminium"></div><div class="field"><label>Material grade</label><input name="grade" placeholder="e.g. EN8D"></div><div class="field"><label>Required delivery</label><input name="date" type="date"></div><div class="field"><label>Delivery location</label><input name="location" value="Ahmedabad, Gujarat"></div><div class="field"><label>Tolerance</label><input name="tolerance" placeholder="±0.05 mm"></div><div class="field"><label>Surface finish</label><input name="finish" placeholder="Ra 1.6 / plating"></div><div class="field"><label>Target price (optional)</label><input name="price" type="number" min="0" step="0.01" placeholder="₹ / unit"></div><div class="field"><label>Certification</label><input name="certification" placeholder="ISO / material cert / inspection"></div><div class="field full"><label>Drawing / specification *</label><textarea name="description" required placeholder="Dimensions, tolerance, special operations, inspection requirements, packaging..."></textarea></div><div class="field full"><label>Drawing file</label><input name="file" type="file" accept=".pdf,.png,.jpg,.jpeg,.dwg,.dxf"></div><div class="field full"><div class="safeNote">🔒 Your personal contact details are not shared with matched suppliers.</div></div><div class="field full"><button class="btn teal" type="submit">Submit & start supplier matching →</button></div></form></div></div>`;document.body.appendChild(m);m.querySelector('#rfqForm').onsubmit=e=>submitRFQ(e,m);}
async function submitRFQ(e,m){e.preventDefault();const f=new FormData(e.target);const file=f.get('file');try{let id;if(live){const payload={title:f.get('title'),description:f.get('description'),quantity:f.get('quantity'),unit:f.get('unit'),material:f.get('material'),material_grade:f.get('grade'),tolerance:f.get('tolerance'),surface_finish:f.get('finish'),certification:f.get('certification'),delivery_location:f.get('location'),required_delivery_date:f.get('date'),target_price:f.get('price'),category_slug:f.get('category')};const {data,error}=await db.rpc('submit_rfq',{p_data:payload});if(error)throw error;id=data;if(file&&file.size){const {data:sess}=await db.auth.getSession();const path=`${sess.session.user.id}/${id}/${Date.now()}-${file.name}`;const up=await db.storage.from('rfq-files').upload(path,file,{upsert:false});if(!up.error)await db.from('rfq_files').insert({rfq_id:id,storage_path:path,file_name:file.name,mime_type:file.type,size_bytes:file.size});}}else{id='demo-'+Date.now();demo.rfqs.push({id,title:f.get('title'),category:categories.find(c=>slugs[c]===f.get('category')),quantity:f.get('quantity'),unit:f.get('unit'),material:f.get('material'),location:f.get('location'),status:'matching'});saveDemo();}m.remove();toast('Requirement submitted — supplier matching started.');go('dashboard');}catch(err){toast(err.message||'Could not submit RFQ');}}

async function openRFQDetail(id){const r=demo.rfqs.find(x=>x.id===id);if(live){const {data,error}=await db.from('rfqs').select('*,categories(name),supplier_matches(*,supplier_profiles(*,companies(name)))').eq('id',id).single();if(error){toast(error.message);return;}renderRFQLive(data);return;}if(!r)return;const matches=[['Rajkot Precision Works','₹148 / pc','2–3 weeks'],['Apex VMC Manufacturing','₹152 / pc','3 weeks'],['Shree CNC Works','₹155 / pc','18 days']];const m=document.createElement('div');m.className='modalWrap';m.innerHTML=`<div class="modal"><div class="modalHead"><div><span class="badge">${esc(r.status)}</span><b>${esc(r.title)}</b><small>${esc(r.category)} · ${esc(r.quantity)} ${esc(r.unit)} · ${esc(r.material)}</small></div><button class="close" onclick="this.closest('.modalWrap').remove()">×</button></div><div class="modalBody"><div class="matchBanner">✓ 3 manufacturers matched based on process & capability</div>${matches.map((x,i)=>`<div class="quoteCard"><div><b>${x[0]}</b><span>Verified manufacturer · ${x[2]}</span></div><strong>${x[1]}</strong><button class="btn" onclick="openQuote('demo')">${i===0?'View quote':'Compare'} →</button></div>`).join('')}</div></div>`;document.body.appendChild(m)}
function renderRFQLive(r){const m=document.createElement('div');m.className='modalWrap';m.innerHTML=`<div class="modal"><div class="modalHead"><div><span class="badge">${esc(r.status)}</span><b>${esc(r.title)}</b><small>${esc(r.categories?.name||'')} · ${esc(r.quantity)} ${esc(r.unit)} · ${esc(r.material)}</small></div><button class="close" onclick="this.closest('.modalWrap').remove()">×</button></div><div class="modalBody"><div class="matchBanner">${r.supplier_matches?.length||0} matched manufacturers</div>${(r.supplier_matches||[]).map(m=>`<div class="quoteCard"><div><b>${esc(m.supplier_profiles?.companies?.name||'Verified manufacturer')}</b><span>Match score ${m.match_score}%</span></div><span class="badge">Invited</span></div>`).join('')||'<div class="empty compact">Matching is running. Verified suppliers will appear here.</div>'}</div></div>`;document.body.appendChild(m)}

function openQuote(rfqId){if(!sessionUser){openAuth('signin','supplier');return;}const existing=demo.quotes.find(q=>q.rfqId===rfqId);const m=document.createElement('div');m.className='modalWrap';m.innerHTML=`<div class="modal"><div class="modalHead"><div><span class="eyebrow">Supplier quotation</span><b>${existing?'Update quote':'Submit a structured quote'}</b><small>Separate costs so buyers can compare fairly.</small></div><button class="close" onclick="this.closest('.modalWrap').remove()">×</button></div><div class="modalBody"><form id="quoteForm" class="formGrid"><div class="field"><label>Unit price *</label><input name="unit" type="number" min="0" step="0.01" required value="${existing?.unitPrice||''}" placeholder="148"></div><div class="field"><label>Quantity *</label><input name="qty" type="number" min="1" step="0.01" required value="${existing?.quantity||1000}"></div><div class="field"><label>Tooling cost</label><input name="tooling" type="number" min="0" value="${existing?.tooling||0}"></div><div class="field"><label>Material cost</label><input name="material" type="number" min="0" value="${existing?.material||0}"></div><div class="field"><label>Setup cost</label><input name="setup" type="number" min="0" value="${existing?.setup||0}"></div><div class="field"><label>Packaging</label><input name="packaging" type="number" min="0" value="${existing?.packaging||0}"></div><div class="field"><label>Inspection</label><input name="inspection" type="number" min="0" value="${existing?.inspection||0}"></div><div class="field"><label>Shipping</label><input name="shipping" type="number" min="0" value="${existing?.shipping||0}"></div><div class="field"><label>GST %</label><input name="gst" type="number" min="0" max="28" value="${existing?.gst||18}"></div><div class="field"><label>Lead time (days) *</label><input name="lead" type="number" min="0" required value="${existing?.lead||18}"></div><div class="field"><label>Payment terms</label><input name="terms" value="${existing?.terms||'30% advance / balance on dispatch'}"></div><div class="field"><label>Quote valid until</label><input name="valid" type="date"></div><div class="field full"><label>Notes</label><textarea name="notes" placeholder="Capacity, material source, inspection plan, assumptions...">${existing?.notes||''}</textarea></div><div class="field full"><div class="quoteTotal"><span>Estimated total before platform fee</span><strong id="quoteTotal">₹0</strong></div></div><div class="field full"><button class="btn teal" type="submit">${existing?'Update quote':'Submit quote'} →</button></div></form></div></div>`;document.body.appendChild(m);const form=m.querySelector('#quoteForm');const calc=()=>{const f=new FormData(form);const q=Number(f.get('qty')||0);const total=q*Number(f.get('unit')||0)+['tooling','material','setup','packaging','inspection','shipping'].reduce((s,k)=>s+Number(f.get(k)||0),0);m.querySelector('#quoteTotal').textContent=money(total);};form.addEventListener('input',calc);calc();form.onsubmit=e=>saveQuote(e,m,rfqId)}
async function saveQuote(e,m,rfqId){e.preventDefault();const f=new FormData(e.target);try{if(live){const {data:sess}=await db.auth.getSession();const {data:sp}=await db.from('supplier_profiles').select('id').eq('user_id',sess.session.user.id).single();if(!sp)throw new Error('Complete your manufacturer profile first.');const row={rfq_id:rfqId,supplier_id:sp.id,unit_price:Number(f.get('unit')),quantity:Number(f.get('qty')),tooling_cost:Number(f.get('tooling')||0),material_cost:Number(f.get('material')||0),setup_cost:Number(f.get('setup')||0),packaging_cost:Number(f.get('packaging')||0),inspection_cost:Number(f.get('inspection')||0),shipping_cost:Number(f.get('shipping')||0),gst_percent:Number(f.get('gst')||18),lead_time_days:Number(f.get('lead')),payment_terms:f.get('terms'),valid_until:f.get('valid')||null,notes:f.get('notes'),status:'submitted'};const {error}=await db.from('quotes').upsert(row,{onConflict:'rfq_id,supplier_id'});if(error)throw error;}else{const q={id:'q-'+Date.now(),rfqId,unitPrice:Number(f.get('unit')),quantity:Number(f.get('qty')),tooling:Number(f.get('tooling')||0),material:Number(f.get('material')||0),setup:Number(f.get('setup')||0),packaging:Number(f.get('packaging')||0),inspection:Number(f.get('inspection')||0),shipping:Number(f.get('shipping')||0),gst:Number(f.get('gst')||18),lead:Number(f.get('lead')),terms:f.get('terms'),notes:f.get('notes'),status:'submitted'};const old=demo.quotes.findIndex(x=>x.rfqId===rfqId);if(old>=0)demo.quotes[old]=q;else demo.quotes.push(q);saveDemo();}m.remove();toast('Quote submitted successfully.');go('dashboard');}catch(err){toast(err.message||'Quote could not be saved');}}

function openOrders(){const m=document.createElement('div');m.className='modalWrap';m.innerHTML=`<div class="modal wide"><div class="modalHead"><div><span class="eyebrow">Buyer orders</span><b>Order Room</b><small>Production, quality, shipment and payment in one timeline.</small></div><button class="close" onclick="this.closest('.modalWrap').remove()">×</button></div><div class="modalBody">${demo.orders.map(orderCard).join('')||'<div class="empty">No orders yet. Accept a supplier quote to create one.</div>'}</div></div>`;document.body.appendChild(m)}
function openSupplierOrders(){openOrders()}
function openOrder(id){const o=demo.orders.find(x=>x.id===id)||demo.orders[0];if(!o){toast('No order yet');return;}const m=document.createElement('div');m.className='modalWrap';m.innerHTML=`<div class="modal wide"><div class="modalHead"><div><span class="badge">${esc(o.status||'in production')}</span><b>${esc(o.orderNumber)}</b><small>${esc(o.title)} · ${esc(o.quantity)} ${esc(o.unit||'pcs')} · ${esc(o.supplier||'Verified manufacturer')}</small></div><button class="close" onclick="this.closest('.modalWrap').remove()">×</button></div><div class="modalBody"><div class="orderHero"><div><span>Order value</span><strong>${money(o.total)}</strong></div><div><span>Platform fee</span><strong>${money(o.platformFee)}</strong></div><div><span>Supplier payout</span><strong>${money(o.payout)}</strong></div></div><div class="progress"><span style="width:${o.progress||62}%"></span></div><div class="timeline">${[['Order confirmed','Completed',1],['Material received','Completed',1],['Production in progress',`${o.progress||62}%`,1],['Quality inspection','Upcoming',0],['Ready for dispatch','Upcoming',0],['Delivered','Upcoming',0]].map(x=>`<div class="tl ${x[2]?'done':''}"><div class="lineDot"></div><div><b>${x[0]}</b><span>${x[1]}</span></div></div>`).join('')}</div><div class="feature"><h3>Documents</h3><p>Material certificate · Inspection report · Measurement report · Dispatch documents</p></div><div class="orderActions"><button class="btn">Messages</button><button class="btn">Report issue</button></div></div></div>`;document.body.appendChild(m)}
function openMessages(){toast('Messaging is wired to the V2 conversations/messages tables in live mode.');}
function adminRFQs(){toast('Admin queue uses RFQs + matches + exceptions from Supabase in live mode.');}
function adminRFQ(id){openRFQDetail(id)}

async function acceptQuote(quoteId){if(!live){const q=demo.quotes.find(x=>x.id===quoteId);const r=demo.rfqs.find(x=>x.id===q?.rfqId);if(!q||!r)return;const sub=q.unitPrice*q.quantity+q.tooling+q.material+q.setup+q.packaging+q.inspection+q.shipping;const gst=sub*q.gst/100;const fee=(sub+gst)*0.02;demo.orders.push({id:'o-'+Date.now(),orderNumber:'IC-2026-'+String(demo.orders.length+1).padStart(6,'0'),title:r.title,quantity:q.quantity,total:sub+gst+fee,platformFee:fee,payout:sub+gst,commission:2,status:'payment_pending',progress:0,payoutStatus:'pending',supplier:'Verified manufacturer'});q.status='accepted';r.status='order_created';saveDemo();toast('Order created with 2% platform fee.');go('dashboard');return;}try{const {data,error}=await db.rpc('create_order_from_quote',{p_quote_id:quoteId,p_commission_percent:2});if(error)throw error;toast('Order created. Payment is pending.');go('dashboard');}catch(e){toast(e.message||'Could not create order');}}

function render(){if(!app)return;if(location.hash==='#dashboard')app.innerHTML=dashboard();else app.innerHTML=home();}
function go(v){location.hash=v==='home'?'':'#'+v;render();window.scrollTo({top:0,behavior:'smooth'});}
window.go=go;window.openRFQ=openRFQ;window.openAuth=openAuth;window.logout=logout;window.openQuote=openQuote;window.openRFQDetail=openRFQDetail;window.openOrders=openOrders;window.openSupplierOrders=openSupplierOrders;window.openOrder=openOrder;window.openMessages=openMessages;window.adminRFQs=adminRFQs;window.adminRFQ=adminRFQ;window.acceptQuote=acceptQuote;
window.addEventListener('hashchange',render);
if(live){db.auth.onAuthStateChange(()=>loadSession());}
loadSession();
